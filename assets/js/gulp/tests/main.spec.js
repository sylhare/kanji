// Tests with jest
// jest --watch // to run the tests
// jest --coverage // to get the coverage

describe("kanji tests", function () {

  test.todo('Well this is not exactly how it should be tested');

  test('Jest', () => {
    expect(true).toBe(true);
    expect(true).toEqual(true);
  });

});
