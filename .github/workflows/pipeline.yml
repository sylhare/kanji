name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Use sylhare/jekyll:ubuntu docker image
      uses: docker://sylhare/jekyll:ubuntu
    - name: Build project  
      run: jekyll build --strict_front_matter
